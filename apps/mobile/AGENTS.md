# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

## Sync
When a migration adds a column to a synced table, follow `docs/SYNC-MIGRATION-CHECKLIST.md`.
